# Copiar a `.env.local` (gitignored) y rellenar.
# En deploys (Vercel / Cursor), define la misma variable en la UI de env.

# Carto basemap / Maps API key.
# Prefijo VITE_ → se expone al cliente; restringe por dominio en Carto.
VITE_CARTO_API_KEY=
